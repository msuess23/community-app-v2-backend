[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "tests", "alembic", "scripts"]
extend-exclude = [".git", ".venv", "venv"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "strict"
